apiVersion: v1
kind: Service
metadata:
  name: {{ include "neguko.fullname" . }}
  labels:
    {{- include "neguko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neguko.selectorLabels" . | nindent 4 }}
