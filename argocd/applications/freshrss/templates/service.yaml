apiVersion: v1
kind: Service
metadata:
  labels:
    {{- with .Values.labels }}
    {{ toYaml . }}
    {{- end }}
  name: {{ include "freshrss.name" . }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: freshrss
  type: ClusterIP
status:
  loadBalancer: {}
