apiVersion: v1
kind: Service
metadata:
  name: {{ include "bambuddy.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "bambuddy.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "bambuddy.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: bambu-studio-api
  namespace: {{ .Values.namespace }}
  labels:
    app: bambu-studio-api
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: bambu-studio-api
